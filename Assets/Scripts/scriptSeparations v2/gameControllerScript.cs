using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class gameControllerScript : MonoBehaviour
{
    //use this to basically say what the different button on a controller would do
    //also make it so that the NPCs can potentially use these button mappings basically, for better NPC behavior scripting

    //target controller type:  x-box [360]

    //also, later can work on ability to "re-map" the controls.
    //or maybe IMMEDIATELY.  i don't currently have an x-box/360 controller, after all.....



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
