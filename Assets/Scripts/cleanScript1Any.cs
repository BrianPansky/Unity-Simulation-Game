using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cleanScript1Any : MonoBehaviour
{
    //this is my attempt to make a script that does need to import a billion other scripts
    //import NO other scripts!
    //[so basically this is for stuff that can potentially operate without necessarily having an AI attached








    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
