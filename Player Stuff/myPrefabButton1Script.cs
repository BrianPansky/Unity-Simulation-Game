using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class myPrefabButton1Script : MonoBehaviour
{
    //public method theFunction;

    /*
    public void pressmyPrefabButton1(Func thisFunction)
    {
        //so, I can pass a function in here and call it?
        //not sure exactly how this needs to work...
        thisFunction();
    }
    */

    public void pressMyPrefabButton1()
    {
        /*
        //hmm, but each function needs access to data, will they have access to the data???
        if(theFunction != null)
        {
            Debug.Log("says this should be working");
            theFunction();
        }
        else
        {
            Debug.Log("function for button is null");
        }
        */
    }

}
